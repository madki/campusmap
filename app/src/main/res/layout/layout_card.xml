<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:custom="http://schemas.android.com/apk/res-auto"
    android:id="@+id/dragView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#eeeeee"
    android:clickable="true"
    android:focusable="false"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/new_small_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/small_card_bg"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/place_color"
            android:layout_width="12dp"
            android:layout_height="fill_parent"
            android:visibility="invisible"/>

        <RelativeLayout
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/hidden_card_height"
            android:paddingBottom="8dp">

            <ImageButton
                android:id="@+id/add_marker_icon"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_alignParentRight="true"
                android:layout_alignParentTop="true"
                android:layout_marginRight="0dp"
                android:layout_marginTop="4dp"
                android:background="@android:color/transparent"
                android:contentDescription="@string/search"
                android:cropToPadding="true"
                android:padding="8dp"
                android:scaleType="fitXY"
                android:src="@drawable/lock_all_off" />

            <in.workarounds.instimap.views.TypeFacedTextView
                android:id="@+id/place_name"
                android:layout_width="fill_parent"
                android:layout_height="wrap_content"
                android:layout_alignParentLeft="true"
                android:layout_alignParentTop="true"
                android:layout_toLeftOf="@id/add_marker_icon"
                android:ellipsize="end"
                android:maxLines="1"
                android:paddingLeft="8dp"
                android:paddingRight="8dp"
                android:paddingTop="8dp"
                android:textSize="@dimen/place_name_text_size"
                custom:typeFace="bold"></in.workarounds.instimap.views.TypeFacedTextView>

            <in.workarounds.instimap.views.SubHeadingView
                android:id="@+id/place_sub_head"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentLeft="true"
                android:layout_below="@id/place_name"
                android:layout_toLeftOf="@id/add_marker_icon"
                android:paddingLeft="8dp"
                android:paddingRight="8dp" />

        </RelativeLayout>
    </LinearLayout>

    <RelativeLayout
        android:id="@+id/card_expand_container"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:background="@color/list_item_gray_even">

        <include
            android:id="@+id/expand_card_view"
            layout="@layout/layout_titled_view_pager" />

        <!--<View-->
            <!--android:layout_width="match_parent"-->
            <!--android:layout_height="8dp"-->
            <!--android:layout_alignParentTop="true"-->
            <!--android:background="@drawable/shadow_gradient" />-->
    </RelativeLayout>
</LinearLayout>